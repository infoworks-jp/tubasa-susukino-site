(()=>{if(!matchMedia('(max-width:700px)').matches)return;const stage=document.getElementById('fluidTextStage');if(!stage)return;const c=document.createElement('canvas');c.className='mobile-fluid-fallback';stage.appendChild(c);const ctx=c.getContext('2d',{alpha:true});let w=0,h=0,dpr=1,blobs=[];const resize=()=>{const r=stage.getBoundingClientRect();w=r.width;h=r.height;dpr=Math.min(devicePixelRatio||1,1.5);c.width=Math.max(1,Math.round(w*dpr));c.height=Math.max(1,Math.round(h*dpr));c.style.width=w+'px';c.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0)};resize();addEventListener('resize',resize,{passive:true});const add=(x,y,p=1)=>{for(let i=0;i<7;i++)blobs.push({x:x+(Math.random()-.5)*28,y:y+(Math.random()-.5)*28,vx:(Math.random()-.5)*.24,vy:(Math.random()-.5)*.18,r:18+Math.random()*34,life:0,max:900+Math.random()*1100,a:.07+Math.random()*.08,p})};let auto=0;add(w*.54,h*.38,1.3);add(w*.62,h*.48,1.1);addEventListener('pointermove',e=>{const r=stage.getBoundingClientRect();if(e.clientY<r.top||e.clientY>r.bottom)return;add(e.clientX-r.left,e.clientY-r.top,.9)},{passive:true});function frame(now){ctx.clearRect(0,0,w,h);if(now-auto>850){add(w*(.48+Math.sin(now*.0007)*.18),h*(.36+Math.cos(now*.0005)*.08),.7);auto=now}for(let i=blobs.length-1;i>=0;i--){const b=blobs[i];b.life+=16;if(b.life>b.max){blobs.splice(i,1);continue}const t=b.life/b.max;b.x+=b.vx+Math.sin(t*8+b.r)*.16;b.y+=b.vy;b.r+=.025;const a=b.a*b.p*(1-t);const g=ctx.createRadialGradient(b.x,b.y,0,b.x,b.y,b.r);g.addColorStop(0,`rgba(215,235,255,${a})`);g.addColorStop(.45,`rgba(150,180,215,${a*.55})`);g.addColorStop(1,'rgba(80,100,130,0)');ctx.fillStyle=g;ctx.beginPath();ctx.arc(b.x,b.y,b.r,0,Math.PI*2);ctx.fill()}requestAnimationFrame(frame)}requestAnimationFrame(frame);window.__tsubasaMobileFluid={engine:'mobile-fluid-fallback',animated:true,scrollSafe:true}})();