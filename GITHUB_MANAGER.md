# 味一番つばさ Web制作・GitHub管理規約

この文書は本リポジトリにおける実装・検品・完了判定の最上位管理規約である。
実装者は「実装者＋品質管理責任者＋原因解析担当」である。
実装者自身の自己申告を完了判定に使用してはならない。
ユーザーに確認作業をさせてはならない。
GitHubへのcommit、コード変更、CSS変更、ファイル配置は作業完了ではない。
公開サイトで要求どおりの結果になったことを確認して初めて完了とする。

## 0. 外部基準実装を使用するWeb制作・絶対実行規約

ユーザーが参考実装・GitHub・デモサイトを指定した場合、「似たものを作る」のではなく、指定された実装を技術的基準として扱う。

最重要命令：
- 勝手に簡略化しない。
- 勝手に別方式へ置換しない。
- 動かなければ原因を調査する。
- 別物を作って解決したことにしない。
- FAILした状態でユーザーへ作業を返さない。

### 0.1 最初に本家そのものを再現する

参考実装が指定された場合、いきなり対象サイト用に改造してはならない。
最初に隔離されたテスト環境で本家を可能な限り無改造で動作させる。
この段階では色変更、エフェクト変更、軽量化、アルゴリズム変更、独自コードへの置換を禁止する。
まず「本家が正常に動く基準状態」を作る。

### 0.2 本家基準状態を証拠として保存する

PC・スマホで本家基準状態を撮影する。
最低限、静止状態、1秒後、3秒後、マウス操作中、マウス操作後を保存する。
動き、渦、拡散、反応範囲を確認し、これを REFERENCE BASELINE とする。

### 0.3 基準状態がPASSするまで改造禁止

REFERENCE BASELINE が正常に動作しない場合、対象サイトへの組み込みへ進んではならない。
FAIL → 原因特定 → 修正 → 再検査を可能な限り自動反復する。

### 0.4 一変更・一検品

REFERENCE BASELINE 完成後、以下の順序で段階的に移植する。複数項目を同時に変更してはならない。

1. 対象写真へ重ねる。流体設定は変更しない。→ 検品
2. 発生位置だけ変更。→ 検品
3. 色だけ変更。→ 検品
4. 発生量だけ変更。→ 検品
5. 速度・渦・拡散を調整。→ 検品
6. マウス反応を調整。→ 検品
7. 最後に性能最適化。→ 検品

### 0.5 デグレードを自動検出する

各STEPは直前のPASS状態と比較する。
変更によって本家の特徴が失われた場合、その変更はFAILとする。
FAILした変更を積み重ねてはならない。
必ず最後のPASS状態へ戻してから別の方法を試す。

### 0.6 「重い」を理由にアルゴリズムを捨てない

性能問題が発生しても、粒子、CSS、GIF、動画、FBMだけの疑似煙、簡易シェーダー等へ勝手に置換してはならない。
まず画面外停止、IntersectionObserver、simulation resolution、DPR、pressure iteration、更新FPS、GPU負荷、Canvasサイズを測定して最適化する。
最適化後に本家らしさが失われた場合はFAILとする。

### 0.7 数字を勘で変更しない

パラメータ変更には必ず理由を持たせる。
「とりあえず半分」「少し強く」「軽そうだから」を禁止する。
変更前値、変更後値、変更理由、視覚差、性能差を記録する。

### 0.8 QAのPASSを信用しすぎない

自動テストPASSだけでは完成ではない。
「ピクセルが変化した」「Canvasが描画された」「FPSが基準内」「マウス前後で差分が出た」だけでは合格にしない。
実際の公開合成画面を画像として確認する。

### 0.9 参考実装との比較を毎回行う

完成候補は REFERENCE BASELINE と現在の公開サイトを比較する。
指定された参考実装の特徴が明らかに失われている場合はFAILとする。
「別物だがそれなりに綺麗」は不合格とする。

### 0.10 FAILしたらユーザーへ返さない

FAILを発見しただけで「FAILでした」「次は○○を直します」とユーザーへ返して作業を停止してはならない。
同一実行機会の中で可能な限り、FAIL → 原因解析 → 修正 → commit → 公開 → 再検品を反復する。
ユーザーに「やれ」を繰り返させない。

### 0.11 原因不明なら変更を増やさない

結果が悪化した場合、新しい調整を重ねて誤魔化してはならない。
最後のPASS状態へ戻し、「何を変更した結果、何が壊れたのか」を一つずつ特定する。

### 0.12 完成条件

以下を全部満たした場合のみ完成とする。
- 本家基準状態 PASS
- 対象サイトへの組込み PASS
- 写真との位置合わせ PASS
- PC視覚品質 PASS
- スマホ視覚品質 PASS
- 自然アニメーション PASS
- マウスインタラクション PASS
- スクロール性能 PASS
- コンソール重大エラーなし
- 参考実装との比較 PASS
- 対象外の部分にデグレードなし

一つでもFAILなら未完成である。

### 0.13 完成報告

完成時だけユーザーへ報告する。
報告には公開URL、commit SHA、REFERENCE BASELINE、PC検品結果、スマホ検品結果、性能測定結果、参考実装との比較結果を付ける。

## 1. ユーザー指示を勝手に簡略化しない

ユーザーの指示はそのまま要求仕様として扱う。
「だいたい」「近い」「おそらく」「これで反映されるはず」「CSS上は正しい」を合格理由にしてはならない。
分からない部分を勝手に補完して実装しない。
画像による完成見本が提示された場合、文章による推測より画像を優先する。

## 2. 見本画像がある場合は必ず数値化する

目測による位置合わせを禁止する。
画像全体の幅・高さ、および対象物の左端X、上端Y、右端X、下端Y、幅、高さ、中心座標をピクセル単位で測定する。
その後、画面サイズに対する割合へ変換する。
位置調整はこの測定値を根拠として行う。
「少し左」「もう少し上」などの指示も変更前後の座標を記録して実行する。

## 3. 実装前に変更前状態を保存する

変更前の公開サイトを必ず記録する。
最低限、PCスクリーンショット、スマホスクリーンショット、対象物の座標、対象物の表示サイズを基準値として保存する。

## 4. 一度に一つだけ変更する

ユーザーが一項目を指示した場合、その項目以外を変更してはならない。
例：「ロゴを左上へ」ならロゴ位置だけ変更し、サイズ、背景、文字、エフェクト、スクロール、他セクション等を変更しない。

## 5. GitHub commitを成功判定に使用しない

ファイル更新成功、commit成功、GitHub Actions成功、CSS値変更成功、HTML更新成功、画像アップロード成功はすべて途中工程である。
これらを理由に「直しました」「完了しました」「反映しました」と報告してはならない。

## 6. 公開URLを必ず検査する

commit後、GitHub Pagesの公開反映を待つ。
その後、実際の公開URLを新規ブラウザ環境で読み込む。
キャッシュされた旧ページを検査してはならない。必要ならcache bustingを使用する。
公開HTMLが参照しているCSS、JavaScript、画像が最新commitのものか確認する。

## 7. 変更前後をピクセル比較する

変更後スクリーンショットを取得し、変更前と変更後を画像比較する。
位置変更なら変更前座標、変更後座標、実移動量を公開画面から測定する。
CSS値から移動量を推定してはならない。ブラウザに描画された実ピクセルを正とする。

## 8. ユーザーの見本とも比較する

見本画像が存在する場合、変更後公開画面とユーザー見本を比較し、位置、サイズ、余白、重なりを測定する。
要求範囲に入っていなければFAILとする。
FAILの場合は完成報告せず、原因を特定して再修正する。

## 9. 「変わっていない」を自動検出する

変更前後の対象領域の画像差分が0、または意図した場所に差分が存在しない場合は変更失敗と判定する。
CSSを書き換えていても公開画面が変わっていなければ失敗である。
CSS読み込み、キャッシュ、詳細度、!important、別CSSによる上書き、JavaScriptによるstyle変更、古いHTML参照、GitHub Pages反映遅延等を調査する。

## 10. 完了報告の条件

以下を全部満たすまで「完了」「直した」「できた」を禁止する。
1. GitHub変更成功
2. 公開反映確認
3. PC実画面確認
4. スマホ実画面確認
5. 変更前後画像比較
6. ユーザー要求との比較
7. 対象以外が壊れていない
8. スクロール・操作性能に悪化がない
9. コンソール重大エラーなし
10. 品質管理判定 PASS
一つでもFAILなら未完成である。

## 11. 完了報告には証拠を付ける

完成時は最低限、commit SHA、公開URL、変更前座標、変更後座標、実際の移動量、PC検査PASS、スマホ検査PASSを提示する。
コード上の予定値ではなく公開画面から測った実測値を報告する。

## 12. 同じ失敗を二度繰り返さない

失敗が発生した場合、修正だけで終わらせない。
「なぜ検査で防げなかったか」を特定し、再発防止検査をGitHub側へ追加する。
同種の失敗を再度人間に発見させた場合、品質管理システム自体がFAILしたものとして扱う。

## 13. 湯気エフェクト・本家構造固定規約

湯気エフェクトの基準実装は PavelDoGreat/WebGL-Fluid-Simulation とする。
「本家風」「PavelDoGreat型」「似た挙動」を理由に別方式へ置換してはならない。

### 13.1 禁止事項
- Canvas 2D 粒子のみの疑似湯気
- ベジェ曲線、CSS animation、動画、GIFによる代替
- FBM/noiseだけで煙の形を直接描く手続き型シェーダー
- velocity/pressure solverを持たない見た目だけの疑似流体
- 実装者判断による「軽量版」「簡易版」「本家風」への変更

### 13.2 必須流体処理
本家を基準として、少なくとも以下の処理系がソース上かつ実行時に存在すること。
- velocity field
- dye/density field
- advection
- divergence
- pressure solve / Jacobi iteration
- gradient subtraction
- curl
- vorticity confinement
- splat injection
- framebuffer ping-pong
- WebGL GPU simulation
これらのいずれかを削除する変更は、ユーザーから明示的な仕様変更がない限りFAILとする。

### 13.3 つばさ用に変更してよい範囲
本家流体計算を維持した上で変更可能なのは、原則として以下だけとする。
- ラーメン写真上の湯気発生座標
- splatの半径、密度、上向き速度、色
- dissipation、vorticity等の見た目調整値
- PC/スマホ別simulation resolution
- 画面外停止など、流体処理自体を別物へ置換しない性能制御

### 13.4 完成する湯気の視覚要件
公開画面で以下をすべて満たすこと。
- 写真にもともと存在する湯気の発生位置から連続して見える
- 静止した白丸、楕円、団子、縄状の線に見えない
- 細い筋が上昇し、蛇行、分岐、巻き、拡散、消失する
- マウスが近づくと既存の湯気が押され、引かれ、渦を作る
- マウスを動かさなくても自然対流が継続する
- エフェクト単体ではなくラーメン写真の一部として成立する

### 13.5 QAの禁止判定
以下だけを理由にPASSしてはならない。
- Canvasのピクセルが時間変化した
- activePixelsが閾値を超えた
- pointer操作前後に差分が出た
- runtime objectにfluidという文字がある
- GitHub Actionsがsuccessになった
本家必須処理の存在確認と、公開合成画面の視覚検品の両方が必要である。
白い塊・丸・線でも差分が動けばPASSする検査は無効とする。

### 13.6 自己申告禁止
実装者が「本家相当」「完全版」「流体になった」と自己評価することを禁止する。
本家構造チェックと公開実画面検品がPASSした場合のみ完成候補とする。

## 14. 今回の追加固定条件：PavelDoGreat REFERENCE BASELINE

基準：PavelDoGreat/WebGL-Fluid-Simulation

最初に本家を無改造に近い状態で REFERENCE BASELINE として動かす。
REFERENCE BASELINE がPASSする前にラーメン用湯気へ改造してはならない。

以下を削除禁止とする。
- velocity
- density/dye
- advection
- divergence
- pressure/Jacobi
- gradient subtraction
- curl
- vorticity confinement
- splat
- framebuffer ping-pong
- WebGL GPU simulation

最終目的は「白い煙を表示すること」ではない。
ラーメン写真にもともと存在する湯気がそのまま流体として動き出したように見え、上昇、蛇行、分岐、巻き、拡散、消失が連続して起こること。
マウスを近づけると本家同様に流体が押され、巻き込まれ、渦が発生すること。
これを満たさないものは、どれだけActionsがPASSしてもFAILとする。

## 最重要原則

参考実装を指定されたら、まず参考実装そのものを理解し再現する。
再現できる前に改造しない。
改造は一項目ずつ行う。
失敗したら最後のPASSへ戻る。
性能改善を理由に別物へしない。
自動テストではなく公開成果物を正とする。
ユーザーを品質管理者にしない。
ユーザーに同じ指示を二度言わせない。
コードではなく公開画面が正解。
commitではなく成果物が正解。
実装者の自己申告を信用しない。
見本があるなら測る。
変更したなら差分を測る。
画面が変わっていなければ何もしていないのと同じ。
「できそう」ではなく「公開画面でできている」を完成とする。
