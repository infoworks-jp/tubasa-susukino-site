# 味一番つばさ
# Steam Lab完全移植・本番スマホ湯気 実装指示書

## 目的

Steam Labで確認済みの湯気表現を、本番サイトのスマホ版へ正確に移植する。
目的は「似せる」ことではなく、テスト版と本番版を同じ実装・同じ挙動・同じ質感にすることである。

対象は以下の3商品とする。

- 究極の味噌ラーメン
- バターコーンラーメン
- つばさラーメン

つばさラーメンについても、写真本来の湯気位置を基準に、Steam Labと同じ流体煙を実装する。

## 絶対禁止事項

- 既存本番エフェクトの数値を少し調整して「移植」と報告しない
- テスト版と異なるシェーダー、表示処理、密度処理を商品ごとに勝手に作らない
- PC版の湯気、PCレイアウト、指定外のセクションを変更しない
- 写真に焼き込まれた湯気を動的湯気と誤認しない
- 1商品の座標を3商品へ使い回さない
- ユーザーにスクリーンショット取得やURL確認を依頼しない
- PASS未確認で「完成」「同じ」「修正済み」と報告しない
- キャッシュや古い配信ディレクトリを確認せず、見えた画面だけで判定しない

## 実装方針

### 1. Steam Labを基準実装として固定

以下を本番スマホ版へそのまま移植する。

- STEP shader
- RENDER shader
- velocity field
- splat処理
- buoyancy / curl / vorticity
- density減衰
- pointerdown / pointermove / pointerup / pointercancel
- touch操作との共存

本番側で別ソルバーを作らない。

### 2. 商品ごとに独立したCanvasと座標変換

3商品それぞれに独立Canvasを用意し、次を商品ごとに定義する。

- 元画像のnaturalWidth / naturalHeight
- `object-fit` / `object-position`
- CSS transform scale / transform-origin
- スープ面または写真本来の湯気根元の画像座標
- 湯気上端の画像座標
- 発生源数、横幅、初速、濃度

ビューポート固定px座標は禁止。画像座標からCanvas座標へ変換する。

### 3. 発生位置

- 究極の味噌ラーメン：実写の「つばさ」文字の上、丼の縁付近
- バターコーンラーメン：実写のスープ面・湯気根元
- つばさラーメン：丼、イクラ、写真本来の湯気根元

各写真を実際に目視し、画像座標を記録してから実装する。

### 4. 指操作

指操作は全画面で反応すること。

- pointerdownで反応開始
- pointermoveでvelocityとdensityを生成
- pointerup / pointercancelで自然に減衰
- 左端、右端、上部、下部、中央で反応確認
- Canvasのpointer-eventsを無効化しない
- `touch-action`でページ全体のスクロールを禁止しない
- 入力座標はCanvas矩形へ変換し、0〜1へclampする

### 5. 写真との融合

目標は「写真へCG煙を載せる」ことではなく、「写真にもともと写っていた湯気が動き始めた」状態。

- 白い円形・円錐・巨大な板状塊は禁止
- 根元は細く、複数筋、分岐、curl、上部拡散
- 写真の黒・光・コントラストへ馴染ませる
- 指操作時も白い縦棒や単一の塊を出さない

## 実装手順

1. 作業開始前にバックアップまたはGit commitを作成
2. Steam Labの基準コードを固定し、ハッシュまたはcommitを記録
3. 本番スマホ専用の独立Canvas実装を作成
4. 3商品の画像座標を個別に測定
5. Steam Labのシェーダーを同一内容で移植
6. 3商品へ個別アンカーを適用
7. pointer/touch処理を全商品へ接続
8. PC版を変更していないことをdiff確認
9. ブラウザキャッシュを無効化し、最新版ファイルを取得確認
10. 同一条件でSteam Labと本番を比較

## 必須Visual QA

最低限390px、可能なら375px / 430pxでも確認する。

### 各商品

- 湯気根元が写真の実際の位置に一致
- 湯気上端まで連続して見える
- テスト版と筋・分岐・量・速度・濃淡が一致
- 指操作で全域が反応
- 指を離した後に自然減衰
- 白い塊、縦棒、スポットライト状表現なし

### システム

- JavaScriptエラーなし
- WebGLエラーなし
- CORS / CSP / MIMEエラーなし
- ページスクロール可能
- ナビゲーションがタップ可能
- WebGL非対応時も写真と文字が表示される
- PC版の表示と湯気が変更されていない

## 合格条件

以下をすべて満たすまで完了報告禁止。

- Steam Labと本番スマホ版を同じ390pxで比較済み
- 3商品すべてで湯気の位置・質感・量・指反応がPASS
- 同じ指軌跡で反応結果を比較済み
- 60秒以上の連続動作で急消失・暴走なし
- Console / WebGL / 404 / CORS / CSP / MIMEがすべて問題なし
- 固定公開URLへ最新版が配信されていることをHTTP取得で確認
- 本番サイトの指定外部分に差分がない

合格前に「ほぼ同じ」「相当」「確認済み」と表現してはならない。

## 自動合格判定

作業者は次の条件をすべて満たすまで、自動で修正・比較・再検証を繰り返す。

1. 3商品それぞれで、動的湯気の根元が写真本来の湯気根元または丼縁の正しい位置にある。
2. 白い円・板・縦棒・大きな塊が1つもない。
3. 湯気が細い複数筋として立ち上がり、分岐して写真内の既存湯気上端へ自然につながる。
4. Steam Labと本番を同じ390px・同じ経過時間で比較し、湯気の量、太さ、明るさ、上昇範囲が同等である。
5. 左右端、上部、下部、中央の5箇所で指をなぞり、すべてで可視の流体反応が発生する。
6. 指を離した後、白い残像や急消失を起こさず、湯気へ自然に戻る。
7. 375px / 390px / 430pxで位置ずれがない。
8. 60秒動作でWebGLエラー、Consoleエラー、context lostがない。
9. PC版と指定外セクションに差分がない。

### テスト版との完成度比較（最優先）

Steam Labは実装の参考ではなく、完成品質の唯一の比較基準とする。

- Steam Labと本番版を、同じ端末幅（375px / 390px / 430px）、同じ経過時間、同じ指軌跡で並べて比較する。
- 比較対象は、発生位置、根元幅、筋の細さ、分岐、curl、上昇速度、上端到達位置、湯気量、明るさ、透明度、指反応、指を離した後の減衰である。
- 「コードが同じ」「入力値が同じ」だけでは合格としない。画面上の完成度がSteam Labと同等であることを必須とする。
- Steam Labより本番版が霧状、弱い、白い塊、縦棒、位置ずれ、反応欠落のいずれかを示した場合はFAILとする。
- 作業者は比較スクリーンショットを自ら取得し、視覚的に一致していると判定できるまで修正を続ける。

いずれか1つでもFAILなら、不合格とし、ユーザーへ完成報告を行わず修正を継続する。

## 最終報告に必ず記載する内容

- 変更ファイル
- Steam Lab基準commit/build
- 3商品のアンカー座標
- 同一比較の結果
- 375 / 390 / 430pxの結果
- 指操作の結果
- 60秒耐久結果
- Console / WebGL結果
- 固定公開URL
- PC版未変更のdiff確認結果
