# 承認済み湯気への指圧追加（2026-09-09）

## 変更範囲と基準

最新指示：SafariとCodex横の表示は良いので、そのまま上の4か所へ指圧を足す。
承認基準は `7a246e27caa3b78e5420432324a055114857f2b6`。
対象は究極の味噌1、バターコーン1、つばさ左右2。8商品、写真、CSS、価格、レイアウト、TOP、店内、地図は変更しない。

視覚方針は今の写真の細い湯気を保持すること。ページ構成は維持。操作時のみ既存の筋が分かれ、なぞった方向へ流れ、離すと対流へ戻る。

## 原因と実装

- 旧入力は1フレームの弱い速度注入だけで、押し続ける状態を保持していなかった。
- 丼上の入力は描画マスクの外へ入り、画面で反応を確認しにくかった。
- 既存のPavel由来pressure/advection/vorticityソルバーへ、操作時だけ速度を注入する。写真や通常の描画シェーダー、自然発生処理は変更しない。
- 押した位置を同じ写真内の最寄りの湯気へ対応づける。つばさの左右は独立した発生座標を使用。
- 速度注入係数は280から1800（上の4か所の入力だけ）。静止押下は18×dt×30の相反する力を2か所へ注入。追加の白い密度は0。
- pointerup/cancel/leave、ウインドウ離脱、非表示化で保持を終了。離した後の追加力は0.35秒まで減衰し、残った流れは既存ソルバーで減衰する。
- 元画像のブラウザ標準ドラッグを上の写真だけ無効にした。イベントはpassive、スクロールのpreventDefaultやtouch-action:noneは追加していない。
- 配信変更は `effects.js`、`steam-loader.js` とHTMLのloader参照バージョンだけ。新しい画像、動画、外部ライブラリ、GPUコンテキストは追加しない。

## QA対象・証拠

1. 無操作の維持：旧版/新版を同じ初期GPU状態・同じ時刻で描画し、4か所すべてCanvasのSHA-256が一致。
2. 押す・保持：同時刻の無入力対照画像と比較。旧版のROI変化率は0.14–0.28%、新版は22–29%。丼下部の変化0px。
3. ドラッグ：実マウス操作後も同時刻の無操作対照と比較。丼下部の変化0px。
4. タップ：ChromiumとWebKitのtouchscreen入力。WebKit 375/390/430pxで全4か所の可視変化と操作終了を確認。
5. スマホ縦スクロール：Chromiumのブラウザ入力で押下→縦移動→離す。155pxスクロールし、保持状態は全てnull。
6. 継続・異常系：111秒で2,786描画、エラー0、保持状態残りなし。既存検査で画面外停止・復帰、reduced-motion、GPU非対応時の原写真、横はみ出し、JS/HTTPエラーを検査。
7. 既存全体：Chrome PC/390px、Brave PC、WebKit 375/390/430pxの全6条件で11写真・12発生源の既存検査PASS。
8. 視覚確認：Safari実アプリの3セクション全4か所でドラッグ前後を確認。ChromeとWebKitの押下/タップ画像も確認。白丸・棒・丼の変形・レイアウト変更は見られなかった。
9. Lighthouse 13、ローカル・モバイル条件のAccessibility/Best Practices/SEO各100。フィールドCore Web Vitalsや実機性能の評価ではない。

再発防止の `tests/steam-pressure-qa.mjs` は入力フラグだけでなく、同じ流体初期状態・同じ時刻の対照画像との差を調べる。実マウスとtouchscreen APIを使用。時間制御用コードは検査ページだけに注入し、本番のeffects.jsには存在しない。通常時間の目視確認は別に行う。

検査記録：`output/pressure-full-qa/report.json`、`output/pressure-local/baseline-comparison.json`、`webkit-pressure.json`、`webkit-other-widths.json`、`soak.json`、`lighthouse.json` と各画像。
スマホはエミュレーションであり実機iPhone/Androidではない。全設定・全端末を保証するものではない。

## 公開判定

この文書の存在を公開成功の根拠にしない。push後に今回のDeploy Pagesと必須QAの成功、公開HTML/JSのバイト一致、公開画面での実操作を確認してから報告する。
